server:
  port: 8002
spring:
  application:
    name: netflix-ribbon
eureka:
  instance:
    hostname: eureka
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka:80/eureka/
